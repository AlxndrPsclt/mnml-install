# Install postmarket os with no interface on the device, make sure ypu can connect by ssh.

#connect to the internet; probably via wifi
# sudo nmtui

sudo apk update
sudo apk upgrade
sudo apk add python3
