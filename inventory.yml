all:
    hosts:
      172.16.42.1
    vars:
      ansible_connection: ssh
      ansible_user: alex
      HOME: "/home/alex"
      LOCAL_HOME: "/home/alex"
      USER: "alex"
      CONFIG_PERSO: "{{ HOME }}/.config/perso"
